const createMessage = (data) => {
  console.log(data);
};

const getMessages = (id) => {
  console.log(id);
};

const sendMessage = async (data) => {
  console.log("send message");
};

module.exports = { createMessage, getMessages, sendMessage };
