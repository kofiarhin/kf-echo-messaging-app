const { users } = require("./data");

const userOne = users[0];
const userTwo = users[1];
const userThree = users[2];

const foundUsers = async ([userOne, userTwo, userThree]) => {};

module.exports = { userOne, userTwo, userThree };
